import { Meta } from '@storybook/addon-docs/blocks';
import { spacing, space } from '@ciberso/tokens';

<Meta title="Foundations/Typography/Baseline Grids" />

# Baseline Grids

Sistema de grid de baseline para alinhamento vertical consistente no Ciberso-UI Design System.

## Overview

O baseline grid é um sistema invisível de linhas horizontais que ajuda a alinhar elementos verticalmente, criando uma aparência mais organizada e profissional.

## Grid System

O Ciberso-UI utiliza um sistema de baseline baseado em **4px**, garantindo alinhamento consistente em todos os componentes.

### Spacing Scale

A escala de espaçamento segue múltiplos de 4px:

<div style={{ marginTop: '2rem' }}>
  <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(100px, 1fr))', gap: '1rem' }}>
    {Object.entries({ ...spacing, ...space }).map(([key, value]) => {
      const sizeInPx = parseFloat(value.replace('rem', '')) * 16;
      return (
        <div key={key} style={{ textAlign: 'center' }}>
          <div
            style={{
              width: '100%',
              height: value,
              backgroundColor: '#007BFF',
              borderRadius: '4px',
              marginBottom: '0.5rem',
            }}
          />
          <div style={{ fontSize: '12px', fontWeight: 'bold' }}>{key}</div>
          <div style={{ fontSize: '11px', color: '#6b7280' }}>{value}</div>
          <div style={{ fontSize: '10px', color: '#9ca3af' }}>{sizeInPx}px</div>
        </div>
      );
    })}
  </div>
</div>

## Line Height Alignment

Os line heights são calculados para alinhar com o baseline grid:

<div style={{ marginTop: '2rem' }}>
  <h3>Text Alignment Examples</h3>
  <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem', marginTop: '1rem' }}>
    <div style={{ border: '1px solid #e5e7eb', borderRadius: '8px', padding: '1rem', backgroundColor: '#f9fafb' }}>
      <div style={{ fontSize: '16px', lineHeight: '24px', marginBottom: '0.5rem' }}>
        Body text (16px / 24px line-height)
      </div>
      <div style={{ fontSize: '11px', color: '#6b7280' }}>
        Alinhado ao grid de 4px (24px = 6 × 4px)
      </div>
    </div>
    
    <div style={{ border: '1px solid #e5e7eb', borderRadius: '8px', padding: '1rem', backgroundColor: '#f9fafb' }}>
      <div style={{ fontSize: '18px', lineHeight: '28px', marginBottom: '0.5rem' }}>
        Large body text (18px / 28px line-height)
      </div>
      <div style={{ fontSize: '11px', color: '#6b7280' }}>
        Alinhado ao grid de 4px (28px = 7 × 4px)
      </div>
    </div>
    
    <div style={{ border: '1px solid #e5e7eb', borderRadius: '8px', padding: '1rem', backgroundColor: '#f9fafb' }}>
      <div style={{ fontSize: '14px', lineHeight: '20px', marginBottom: '0.5rem' }}>
        Small body text (14px / 20px line-height)
      </div>
      <div style={{ fontSize: '11px', color: '#6b7280' }}>
        Alinhado ao grid de 4px (20px = 5 × 4px)
      </div>
    </div>
  </div>
</div>

## Visual Grid Example

<div style={{ marginTop: '2rem' }}>
  <h3>Baseline Grid Visualization</h3>
  <div style={{ position: 'relative', border: '1px solid #e5e7eb', borderRadius: '8px', padding: '2rem', backgroundColor: '#fff' }}>
    {/* Grid lines */}
    {Array.from({ length: 20 }, (_, i) => (
      <div
        key={i}
        style={{
          position: 'absolute',
          top: `${i * 4}px`,
          left: 0,
          right: 0,
          height: '1px',
          backgroundColor: i % 4 === 0 ? '#e5e7eb' : '#f3f4f6',
          opacity: 0.5,
          pointerEvents: 'none',
        }}
      />
    ))}
    
    {/* Sample content aligned to grid */}
    <div style={{ position: 'relative', zIndex: 1 }}>
      <h2 style={{ fontSize: '24px', lineHeight: '32px', marginTop: '0', marginBottom: '16px' }}>
        Heading Example
      </h2>
      <p style={{ fontSize: '16px', lineHeight: '24px', marginBottom: '16px' }}>
        This paragraph is aligned to the baseline grid. The line height of 24px ensures proper vertical rhythm.
      </p>
      <p style={{ fontSize: '16px', lineHeight: '24px', marginBottom: '0' }}>
        Multiple paragraphs maintain consistent spacing, creating a harmonious reading experience.
      </p>
    </div>
  </div>
</div>

## Implementation Guidelines

### 1. Use Line Heights Múltiplos de 4px

```css
/* ✅ DO: Line heights alinhados ao grid */
.text-base {
  font-size: 16px;
  line-height: 24px; /* 6 × 4px */
}

/* ❌ DON'T: Line heights não alinhados */
.text-base {
  font-size: 16px;
  line-height: 23px; /* Não é múltiplo de 4px */
}
```

### 2. Use Spacing Múltiplos de 4px

```css
/* ✅ DO: Spacing alinhado ao grid */
.component {
  margin-bottom: 16px; /* 4 × 4px */
  padding: 12px; /* 3 × 4px */
}
```

### 3. Mantenha Consistência Vertical

- Margens entre elementos: múltiplos de 4px
- Padding interno: múltiplos de 4px
- Line heights: múltiplos de 4px

## Benefits

1. **Consistência Visual**: Elementos alinhados criam uma aparência mais profissional
2. **Legibilidade**: Espaçamento adequado melhora a leitura
3. **Manutenibilidade**: Sistema previsível facilita desenvolvimento
4. **Escalabilidade**: Fácil adicionar novos elementos mantendo o alinhamento

